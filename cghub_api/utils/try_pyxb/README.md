Use `genbindings_*` to generate corresponding bindings. Experiment, analysis, and run schemas require common schema to be generated first.

Use `parse.py` to parse a test file with generated bindings.
